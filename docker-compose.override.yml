services:
  api:
    volumes:
      - ./src:/app/src
    command:
      [
        "uvicorn",
        "src.main:app",
        "--reload",
        "--host",
        "0.0.0.0",
        "--port",
        "8000"
      ]
    environment:
      APP_ENV: development